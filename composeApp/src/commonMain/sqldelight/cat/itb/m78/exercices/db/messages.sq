CREATE TABLE messages (
  id INTEGER PRIMARY KEY NOT NULL,
  text TEXT NOT NULL
);
CREATE INDEX messages_full_name ON messages(text);
selectAll:
SELECT * FROM messages;
insert:
INSERT INTO messages(text) VALUES (?);